const card1BtnEl = document.querySelector(".card1");
const card2BtnEl = document.querySelector(".card2");
const card3BtnEl = document.querySelector(".card3");
const card4BtnEl = document.querySelector(".card4");

function getAchievementByGame() {
    // fetch all achievements from that game
    // filter out incompleted games, so only completed achievements are left
    // pick a random achievement from that list/array
}

card1BtnEl.addEventListener("click", getAchievementByGame);
card2BtnEl.addEventListener("click", getAchievementByGame);
card3BtnEl.addEventListener("click", getAchievementByGame);
card4BtnEl.addEventListener("click", getAchievementByGame);